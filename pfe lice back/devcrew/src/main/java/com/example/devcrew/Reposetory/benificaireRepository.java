package com.example.devcrew.Reposetory;

import com.example.devcrew.entities.benificaire;
import org.springframework.data.jpa.repository.JpaRepository;

public interface benificaireRepository extends JpaRepository<benificaire,Long> {
}
